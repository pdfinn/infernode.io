---
import { useTranslations, p, type Lang } from '../i18n/utils';

interface Props {
  lang?: Lang;
}
const { lang = 'en' } = Astro.props;
const t = useTranslations(lang).security;
---

<section id="security" class="px-6 py-24 lg:py-32">
  <div class="mx-auto max-w-5xl">
    <div class="grid items-center gap-12 lg:grid-cols-2">
      <!-- Left: explanation -->
      <div>
        <p class="mb-4 text-sm font-medium tracking-widest uppercase text-accent">
          {t.eyebrow}
        </p>
        <h2 class="mb-6 text-3xl font-bold tracking-tight sm:text-4xl" set:html={t.title} />
        <div class="space-y-4 text-text-muted">
          <p set:html={t.p1} />
          <p set:html={t.p2} />
          <p set:html={t.p3} />
          <p set:html={t.p4} />
          <a
            href={p('/security', lang)}
            class="inline-flex items-center gap-2 text-sm font-medium text-accent transition hover:text-accent/80"
            set:html={t.link}
          />
        </div>
      </div>

      <!-- Right: terminal visualization -->
      <div class="rounded-xl border border-border bg-bg-surface font-mono text-sm overflow-hidden">
        <div class="flex items-center gap-2 border-b border-border px-4 py-3">
          <div class="h-3 w-3 rounded-full bg-node-rpi/60"></div>
          <div class="h-3 w-3 rounded-full bg-node-llm/60"></div>
          <div class="h-3 w-3 rounded-full bg-node-jetson/60"></div>
          <span class="ml-2 text-xs text-text-muted">{t.terminalLabel}</span>
        </div>
        <div class="p-5 space-y-3">
          <div>
            <span class="text-accent">;</span>
            <span class="text-text-muted"> {t.term1}</span>
          </div>
          <div>
            <span class="text-accent">;</span>
            <span class="text-text"> mount /n/docs /agent/data</span>
          </div>
          <div>
            <span class="text-accent">;</span>
            <span class="text-text"> mount /n/llm /agent/llm</span>
          </div>
          <div class="mt-4">
            <span class="text-accent">;</span>
            <span class="text-text-muted"> {t.term2}</span>
          </div>
          <div>
            <span class="text-accent">;</span>
            <span class="text-text"> ls /agent/</span>
          </div>
          <div>
            <span class="text-node-laptop">data/</span>
            <span class="text-node-llm ml-4">llm/</span>
          </div>
          <div class="mt-4">
            <span class="text-accent">;</span>
            <span class="text-text-muted"> {t.term3}</span>
          </div>
          <div>
            <span class="text-accent">;</span>
            <span class="text-text"> ls /home/ /credentials/ /email/</span>
          </div>
          <div>
            <span class="text-text-muted italic">
              &nbsp;&nbsp;{t.term4}
            </span>
          </div>
          <div class="mt-4">
            <span class="text-accent">;</span>
            <span class="text-text-muted"> {t.term5}</span>
          </div>
          <div>
            <span class="text-accent">;</span>
            <span class="text-text"> cat /home/.ssh/id_rsa</span>
          </div>
          <div>
            <span class="text-text-muted italic">
              &nbsp;&nbsp;{t.term6}
            </span>
          </div>
        </div>
      </div>
    </div>

    <!-- Post-Quantum Cryptography -->
    <div class="mt-12 rounded-xl border border-border bg-bg-surface p-8">
      <div class="flex flex-col gap-6 sm:flex-row sm:items-start sm:gap-8">
        <div class="flex h-12 w-12 flex-shrink-0 items-center justify-center rounded-lg bg-accent/10">
          <span class="text-xl text-accent">&#9883;</span>
        </div>
        <div>
          <div class="mb-3 flex flex-wrap items-center gap-3">
            <h3 class="text-lg font-semibold">{t.pqTitle}</h3>
            <span class="rounded-full border border-accent/40 px-2.5 py-0.5 text-xs font-medium text-accent">{t.pqBadge}</span>
          </div>
          <p class="mb-4 text-sm leading-relaxed text-text-muted" set:html={t.pqP1} />
          <p class="text-sm leading-relaxed text-text-muted" set:html={t.pqP2} />
        </div>
      </div>
    </div>
  </div>
</section>
